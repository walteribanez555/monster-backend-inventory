



const tableName = "inputs";
const keyField = "input_id";

const queryParams = ["id", "init", "end", "product_id", "warehouse_id", "product_type_id"];

const model = {
  // product_id: "number",
  provider_id: "number",
  quantity: "number",
  detail: "string",
};


export async function getPreparationItem({}){

}

export async function postPreparationItem({}) {
    
}

export async function putPreparationItem({}) {
    
}

export async function deletePreparationItem({}) {
    
}